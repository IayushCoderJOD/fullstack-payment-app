import React from 'react'

const Button = ({ label }) => {
    return (
        <div className='border p-1 text-center font-semibold bg-blue-900  text-gray-100 rounded-md ' >{label}</div>
    )
}

export default Button